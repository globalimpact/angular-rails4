<h1><%= @user.name %></h1>

<p>
<%= @user.email %>
</p>

<%= link_to 'All Users', users_path, :class=> "button round small secondary" %> 
<%= link_to 'Edit User', edit_user_path(@user), :class=> "button round small secondary" %> 
<%= link_to 'Delete User', user_path(@user), :method => 'delete', :confirm => 'Are you sure?', :class=> "button round small secondary" %>