<%= link_to 'New User', new_user_path, :class => "button success round right" %>
<h1>Users</h1>
<table style="width:100%;">
  <thead>
    <tr>
      <th>Name</th>
      <th>Email</th>
      <th>Last Signed In</th>
      <th>Actions</th>
    </tr>
  </thead>
  <tbody>
    <% @users.each do |user| %>
      <tr>
        <td><%= user.name %></td>
        <td><%= link_to user.email, user %></td>
        <td><%= user.last_sign_in_at %></td>
        <td>
          <%= link_to 'Edit', edit_user_path(user), :class => "button small round secondary" %>
          <%= link_to 'Delete', user_path(user), :method => 'delete', :confirm => "Delete this item (" + user.email + ")?", :class => "button small round secondary" %>
        </td>
      </tr>
    <% end %>
  </tbody>
</table>

